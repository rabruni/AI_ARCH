  test-sos:
    if: ${ github.event.inputs.test_sos == 'true' }}
    runs-on: ubuntu-latest
    needs: boot-assist
    steps:
      - name: Simulate System SOS
        run: |
          echo "🔊 Simulating verification failure..."
          exit 1
