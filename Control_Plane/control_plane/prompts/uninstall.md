# Uninstall Prompt Template

You are executing an **UNINSTALL** operation for a selected registry row.

## Steps
1. Determine if other selected modules depend on this one.
2. If safe, remove artifacts/packages.
3. Update registry `status` and notes.
