# Update Prompt Template

You are executing an **UPDATE** operation for a selected registry row.

## Steps
1. Compare `version_current` vs `version_target`.
2. Apply minimal diffs to artifacts.
3. Run/require `verify` operation.
4. Update registry fields and notes.
