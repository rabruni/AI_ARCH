# Verify Prompt Template

You are executing a **VERIFY** operation for a selected registry row.

## Steps
1. Run relevant tests/checks for this module (lint, unit, evals, security).
2. Ensure artifacts exist and match expected structure.
3. Fail if CI gate is required and verification cannot be proven.

## Output
- Evidence (commands run, results)
