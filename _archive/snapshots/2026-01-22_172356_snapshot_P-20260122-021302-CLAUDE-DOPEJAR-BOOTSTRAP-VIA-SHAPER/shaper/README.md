# Shaper CLI

Minimal CLI tool that captures a WORK_ITEM.md from conversation using exact triggers.

Run:
```
python3 -m shaper.cli
```

Triggers (case-insensitive, exact match):
- show me what you have
- turn this into a work item
- freeze it
