# Repo OS — Local Git Hooks (v1)

Purpose:
Prevent violations before they reach CI.

Installs:
- pre-commit hook blocking edits to v1 prompts
- warning if new prompt added without index.csv update
- install-hooks.sh helper

Outcome:
Local guardrails mirror CI rules.
